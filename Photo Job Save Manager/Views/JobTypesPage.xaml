<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Photo_Job_Save_Manager.Views.JobTypesPage"
             Title="Job Types">
    <ContentPage.Content>
        <VerticalStackLayout Padding="20">
            <Label Text="Job Types" FontSize="32" FontAttributes="Bold" />
            <Button Text="Add Job Type" Margin="0,20,0,0" Command="{Binding AddJobTypeCommand}" />
            <CollectionView ItemsSource="{Binding JobTypes}" Margin="0,20,0,0" SelectionMode="Single" SelectionChanged="OnJobTypeSelected">
                <CollectionView.EmptyView>
                    <Label Text="No job types yet." HorizontalOptions="Center" VerticalOptions="Center" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="0,5" Padding="10" BorderColor="#ccc" CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <VerticalStackLayout Grid.Column="0">
                                    <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="20" />
                                    <Label Text="{Binding Fields.Count, StringFormat='Fields: {0}'}" FontSize="14" />
                                </VerticalStackLayout>
                                <Button Grid.Column="1" 
                                        Text="Delete" 
                                        BackgroundColor="Red" 
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteJobTypeCommand}"
                                        CommandParameter="{Binding}"
                                        Margin="10,0,0,0"
                                        Padding="10,5" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage> 